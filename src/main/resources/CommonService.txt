package ${basePackage}.${pckNameLower}.service;

import ${basePackageImport}.model.Paging;
import ${basePackageImport}.model.dto.${pckNameLower}.${ettFirstUpperCamel}Dto;
import ${basePackageImport}.model.dto.exception.BusinessException;
import ${basePackageImport}.model.dto.request.ApproveRequest;
import ${basePackageImport}.model.dto.request.Search${ettFirstUpperCamel}Request;
import ${basePackageImport}.model.dto.response.UploadFileResponse;
import ${basePackageImport}.model.entity.${pckNameLower}.${ettFirstUpperCamel};
import org.springframework.data.domain.Pageable;
import org.springframework.web.multipart.MultipartFile;

import java.io.IOException;
import java.util.List;

public interface ${pckFirstUpperCamel}CommonService {

    UploadFileResponse upload(MultipartFile file, String data, Long id, String username) throws IOException, BusinessException;

    String approve(ApproveRequest request, String username, List<String> roles) throws BusinessException;

    String delete(Long id, List<String> roles) throws BusinessException;

    Object download(Long documentId) throws BusinessException, IOException;

    Document findById(Long id);

    Paging<DocumentDto> advanceSearch(Search${ettFirstUpperCamel}Request searchRequest, Pageable pageable, List<String> roles);
}
