package ${basePackage}.${pckNameLower}.service.adapter;

import ${basePackageImport}.model.dto.exception.BusinessException;
import ${basePackageImport}.model.dto.upload.CheckUploadDto;
import ${basePackageImport}.model.entity.document.Document;

import java.io.IOException;
import java.util.Date;
import java.util.List;

/**
 * @author khanhtd
 * @Date 2023
 */
@SuppressWarnings("rawtypes")
public interface ${pckFirstUpperCamel}CommonAdapter {
    CheckUploadDto checkUpload(${pckFirstUpperCamel} ${pckNameLower}) throws IOException, BusinessException;

    boolean saveList(List<?> list, String username);

    boolean cleanAllById(Long ${pckNameLower}Id);

    boolean updateStatus(Long ${pckNameLower}Id, String status);

    boolean updateApproveInfo(Long ${pckNameLower}Id, String approver, Date approvedDate);

    String getSupportService();
}
