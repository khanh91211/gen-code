package ${basePackage}.${pckNameLower}.repo;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.stereotype.Repository;
import ${basePackageImport}.model.entity.${pckNameLower}.${ettFirstUpperCamel};

/**
 * @author khanhtd
 * @Date 2023
 */
@Repository
#foreach($ppt in $ppts)
#if($textUtils.wordsToLowerCase($ppt.idType) == 'uuid' || $textUtils.wordsToLowerCase($ppt.idType) == 'seq')
#set($typeId = $textUtils.toPascalCase($ppt.type))
#end
#end
public interface ${ettFirstUpperCamel}Repository extends JpaRepository<${ettFirstUpperCamel}, ${typeId}> , JpaSpecificationExecutor<${ettFirstUpperCamel}>{
}
