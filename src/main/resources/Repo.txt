package ${basePackage}.${ettNameLower}.repo;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.stereotype.Repository;
import ${basePackageImport}.model.entity.${ettNameLower}.${ettFirstUpperCamel};

@Repository
public interface ${ettFirstUpperCamel}Repo extends JpaRepository<${ettFirstUpperCamel}, String> , JpaSpecificationExecutor<${ettFirstUpperCamel}>{
}
