package ${base.basePackage}.dto;

import lombok.Getter;
import lombok.Setter;

import javax.validation.constraints.NotNull;
import java.io.Serializable;
import java.util.Date;

@Getter
@Setter
public class ${mdl.name}Request {
#foreach($model in $models)
    #if($model.module == ${mdl.name} && $model.isRequest == 'TRUE')

    #if($model.notNull == 'TRUE')
    @NotNull
    #end
    private ${model.type} ${model.fieldCamel};
    #end
#end

}
