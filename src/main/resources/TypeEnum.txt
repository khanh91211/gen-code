package ${basePackageImport}.model.enumeration;

import lombok.Getter;

public enum ${pckFirstUpperCamel}TypeEnum {
#if($listEntityModule.isEmpty() || $listEntityModule.size() == 0)
#else
#foreach($et in $ettsOfPck)
#set($codeEnum = $textUtils.wordsToSnakeUpper(${et.lineKey}).concat("_").concat($textUtils.wordsToSnakeUpper(${et.typeKey})).concat("_").concat($textUtils.wordsToSnakeUpper(${et.entityKey})))
#if($foreach.last)
    ${codeEnum}("${codeEnum}", "${et.nameDisplay}");
#else
    ${codeEnum}("${codeEnum}", "${et.nameDisplay}"),
#end
#end

    @Getter
    private final String code;

    @Getter
    private final String name;

    private ${pckFirstUpperCamel}TypeEnum(String code, String name) {
        this.code = code;
        this.name = name;
    }
#end
}